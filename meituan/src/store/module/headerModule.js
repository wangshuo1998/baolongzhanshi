let state = {
  city:"北京"//城市的假数据
};

let mutations = {
  changeIndexCityMutation(state,data){
    state.city = data;
  }
};

let actions = {

};

let getters = {

};

export default {
  state,
  mutations,
  actions,
  getters
}
